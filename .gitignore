# Python virtual environment
venv/
__pycache__/
*.pyc

# Data files (too large for GitHub, download from Kaggle instead)
data/
*.csv

# Environment / secrets
.env

# OS junk
.DS_Store
Thumbs.db

# Jupyter checkpoints
.ipynb_checkpoints/

# Large intermediate CSV artifacts (reproducible by running the notebooks)
artifacts/*.csv

# Keep small useful artifacts (model, results, chart)
!artifacts/*.pkl
!artifacts/*.txt
!artifacts/*.png